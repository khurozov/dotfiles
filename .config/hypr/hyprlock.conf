animations {
    enabled = false
}

background {
    path = /tmp/wall.current
    blur_passes = 2
}

input-field {
    size = 400, 60
    outline_thickness = 0
    dots_spacing = 0.5
    inner_color = rgba(1a1b2688)
    font_color = rgb(c0caf5)
    font_family = Iosevka Fixed
    placeholder_text = 
}

label {
    monitor =
    text = $TIME
    color = rgb(c0caf5)
    font_size = 40
    font_family = Iosevka Fixed
    halign = center
    valign = center
    position = 0, 80
}

label {
    monitor =
    text = $LAYOUT
    color = rgba(c0caf5aa)
    font_size = 10
    font_family = Iosevka Fixed
    halign = right
    valign = top
}
